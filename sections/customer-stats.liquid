<div class="section-background color-{{ section.settings.color_scheme }}"></div>

<div
  class="
    section
    section--{{ section.settings.section_width }}
    color-{{ section.settings.color_scheme }}
    customer-stats
    spacing-style
  "
  style="
    {%- render 'spacing-style', settings: section.settings -%}
    --cs-bg: {{ section.settings.background_color }};
    --cs-heading-color: {{ section.settings.heading_color }};
    --cs-ring-color: {{ section.settings.ring_color }};
    --cs-ring-track: {{ section.settings.ring_track_color }};
  "
>
  {% if section.settings.heading != blank %}
    <div class="customer-stats__heading">{{ section.settings.heading }}</div>
  {% endif %}

  {% if section.blocks.size > 0 %}
    <div class="customer-stats__grid">
      {% for block in section.blocks %}
        {% if block.type == 'stat' %}
          {% assign pct = block.settings.percentage | default: 0 %}
          {% assign remaining = 100 | minus: pct %}
          {% assign dash_offset = 283 | times: remaining | divided_by: 100 %}
          <div class="customer-stats__item" {{ block.shopify_attributes }}>
            <div class="customer-stats__ring">
              <svg viewBox="0 0 100 100" width="100" height="100" aria-hidden="true">
                <circle cx="50" cy="50" r="45" fill="none" stroke="var(--cs-ring-track, #E7E6E2)" stroke-width="8" />
                <circle
                  cx="50" cy="50" r="45"
                  fill="none"
                  stroke="var(--cs-ring-color, var(--brand-ideal-orange, #F38B00))"
                  stroke-width="8"
                  stroke-linecap="round"
                  stroke-dasharray="283"
                  stroke-dashoffset="{{ dash_offset }}"
                  transform="rotate(-90 50 50)"
                />
              </svg>
              <span class="customer-stats__pct">{{ pct }}%</span>
            </div>

            <div class="customer-stats__text">
              {% if block.settings.title != blank %}
                <h3 class="customer-stats__title">{{ block.settings.title | escape }}</h3>
              {% endif %}
              {% if block.settings.description != blank %}
                <p class="customer-stats__desc">{{ block.settings.description | escape }}</p>
              {% endif %}
            </div>
          </div>
        {% endif %}
      {% endfor %}
    </div>
  {% endif %}

  {% if section.settings.footnote != blank %}
    <p class="customer-stats__footnote">{{ section.settings.footnote | escape }}</p>
  {% endif %}
</div>

{% stylesheet %}
  .customer-stats {
    display: flex;
    flex-direction: column;
    gap: 24px;
    background-color: var(--cs-bg, #FFFFFF);
  }

  .customer-stats__heading {
    font-family: var(--font-heading--family);
    font-weight: 700;
    font-size: var(--font-size--3xl, 2rem);
    line-height: 40px;
    color: var(--cs-heading-color, var(--brand-aqua-900, #311C01));
    margin: 0;
  }

  .customer-stats__heading p {
    margin: 0;
  }

  .customer-stats__grid {
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .customer-stats__item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 16px;
    padding: 24px 0;
    text-align: center;
  }

  .customer-stats__ring {
    position: relative;
    width: 100px;
    height: 100px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .customer-stats__ring svg {
    position: absolute;
    top: 0;
    left: 0;
  }

  .customer-stats__pct {
    font-family: var(--font-heading--family);
    font-weight: 600;
    font-size: var(--font-size--3xl, 2rem);
    line-height: 40px;
    color: var(--cs-heading-color, var(--brand-aqua-900, #311C01));
    text-align: center;
    position: relative;
  }

  .customer-stats__text {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .customer-stats__title {
    font-family: var(--font-body--family);
    font-weight: 600;
    font-size: var(--font-size--lg, 1.125rem);
    line-height: 28px;
    color: var(--cs-heading-color, var(--brand-aqua-900, #311C01));
    margin: 0;
    text-align: center;
  }

  .customer-stats__desc {
    font-family: var(--font-body--family);
    font-weight: 400;
    font-size: var(--font-size--base, 1rem);
    line-height: 24px;
    color: var(--color-foreground, #121211);
    margin: 0;
    text-align: center;
  }

  .customer-stats__footnote {
    font-family: var(--font-body--family);
    font-weight: 600;
    font-size: 14px;
    line-height: 20px;
    color: #767676;
    margin: 0;
  }

  @media screen and (min-width: 769px) {
    .customer-stats__grid {
      flex-direction: row;
      justify-content: center;
      gap: 48px;
    }

    .customer-stats__item {
      flex: 1;
      max-width: 360px;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:names.customer_stats",
  "tag": "section",
  "settings": [
    {
      "type": "richtext",
      "id": "heading",
      "label": "t:settings.heading",
      "default": "<p>Real Results: What Customers are Saying</p>"
    },
    {
      "type": "text",
      "id": "footnote",
      "label": "Footnote",
      "default": "*Based on an email survey of 472 AquaTru customer respondents."
    },
    {
      "type": "header",
      "content": "t:content.colors"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "t:settings.background_color",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "heading_color",
      "label": "Heading color",
      "default": "#311C01"
    },
    {
      "type": "color",
      "id": "ring_color",
      "label": "Ring color",
      "default": "#F38B00"
    },
    {
      "type": "color",
      "id": "ring_track_color",
      "label": "Ring track color",
      "default": "#E7E6E2"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:settings.color_scheme",
      "default": "scheme-1"
    },
    {
      "type": "select",
      "id": "section_width",
      "label": "t:settings.width",
      "options": [
        { "value": "page-width", "label": "t:options.page" },
        { "value": "full-width", "label": "t:options.full" }
      ],
      "default": "page-width"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "t:settings.padding_top",
      "min": 0,
      "max": 200,
      "step": 4,
      "default": 64,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "t:settings.padding_bottom",
      "min": 0,
      "max": 200,
      "step": 4,
      "default": 64,
      "unit": "px"
    }
  ],
  "blocks": [
    {
      "type": "stat",
      "name": "Stat",
      "settings": [
        {
          "type": "number",
          "id": "percentage",
          "label": "Percentage",
          "default": 98
        },
        {
          "type": "text",
          "id": "title",
          "label": "t:settings.heading",
          "default": "Stat title"
        },
        {
          "type": "textarea",
          "id": "description",
          "label": "t:settings.description",
          "default": "Stat description text."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "t:names.customer_stats",
      "blocks": [
        {
          "type": "stat",
          "settings": {
            "percentage": 98,
            "title": "Cleaner, Safer, Healthier Water",
            "description": "98% of customers agree their drinking water is cleaner, safer, & healthier."
          }
        },
        {
          "type": "stat",
          "settings": {
            "percentage": 98,
            "title": "Water You Can Trust",
            "description": "Over 98% of customers say AquaTru gives them water they can trust."
          }
        },
        {
          "type": "stat",
          "settings": {
            "percentage": 96,
            "title": "Best-Tasting Water",
            "description": "Over 96% of customers say AquaTru is the best water they've ever tasted."
          }
        }
      ]
    }
  ]
}
{% endschema %}
